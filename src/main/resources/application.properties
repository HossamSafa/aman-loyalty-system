server.port=8080
server.servlet.context-path=/api/v1/loyalty

# TEMPORARY - FOR LOCAL POSTMAN TESTING ONLY.
# Activates the test-only clientId bypass (see TestOnlyClientIdentityResolver).
# REMOVE this line before any real integration/production run.
spring.profiles.active=local-test

spring.application.name=loyalty.service

spring.datasource.url=jdbc:postgresql://localhost:5432/aman_loyalty
spring.datasource.username=postgres
spring.datasource.password=112007
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect